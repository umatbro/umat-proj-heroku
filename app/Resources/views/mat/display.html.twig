{% extends 'base.html.twig' %}

{% block body %}
    <div class="container" style="text-align:center; margin-top:40px">
        {% for item in users %}
        <h3>{{ item }}</h3>
        {% endfor %}
    </div>
{% endblock %}