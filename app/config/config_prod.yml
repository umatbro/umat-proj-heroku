imports:
    - { resource: config.yml }

#doctrine:
#    orm:
#        metadata_cache_driver: apc
#        result_cache_driver: apc
#        query_cache_driver: apc

monolog:
    handlers:
        main:
            type: fingers_crossed
            action_level: error
            handler: nested
        nested:
            type: stream
            path: '%kernel.logs_dir%/%kernel.environment%.log'
            level: debug
        console:
            type: console
doctrine:
    dbal:
        driver: pdo_mysql
        host: db4free.net
        port: '%database_port%'
        dbname: umat
        user: umat
        password: welcometothericefield

framework:
  session:
    save_path: "%kernel.root_dir%/../var/sessions/%kernel.environment%"